# 版本变更记录

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.0] - 2026-09-23

把入口**并进视频页自己的「更多」菜单**（⋯），不再只是一个外挂按钮。

### 新增

- **并入 B 站原生「更多」菜单**：菜单项追加在 B 站自己的项之后，并复用它的 `dropdown-item` / `video-toolbar-item-icon` 类，所以间距、字号、hover、深色主题全部自动跟随原生。
  - `竖滑模式刷视频`（带 `Alt+Shift+B` 提示，直接把当前视频插到队首先播）
  - `在竖滑里看评论`（视频流没开就先把它开起来再拉评论抽屉）
  - `只刷这个 UP 主的视频`
  - `不感兴趣（从队列移除）`
  - `复制视频链接`
- **同款兜底菜单**：点互动栏那颗「竖滑刷」时优先去点开 B 站自己的「更多」；如果菜单结构变了/找不到，就弹一个自己画的菜单，参数照抄 B 站 `.video-tool-more-popover`（`min-width` / `padding: 12px 0` / `border-radius: 8px` / `box-shadow: 0 0 30px rgba(0,0,0,.1)`），同样支持深色主题。
- 菜单是点开才渲染的，所以用 MutationObserver 在菜单出现时补注入；菜单项点击后会尝试收起原生菜单（`mouseleave` + 短暂隐藏兜底）。

### 修复

- 从菜单打开视频流时，`openFeed` 没有把 `bvid` 透传下去，导致「先播当前视频」在菜单入口下失效（顶栏入口正常）。已修并补了断言。

### 测试

- `test/entry.test.mjs` 扩到 **22 条**：新增原生菜单注入位置与项数、重复注入只有一组、原生菜单不存在时返回 false 不报错、兜底菜单项数与可关闭、点击「竖滑模式」真的带上了当前 BV、四个动作的分发、原生菜单项点击也能触发动作、以及菜单样式参数必须与 B 站一致。
- demo 页补了仿「更多」菜单结构，可肉眼验证注入效果。

## [1.2.0] - 2026-09-23

工具栏弹窗里加上**点赞 / 投币 / 收藏 / 评论**，不用进视频流也能直接互动。

### 新增

- **弹窗互动区**：显示当前目标视频的封面、标题、UP 主，以及四个按钮——`赞`/`币`/`藏`/`评`，按钮上直接带计数；已赞/已投/已藏会高亮，投币后按钮置灰（B 站不支持撤币）。
- **目标视频的选取规则**：优先取**正在竖滑播放的那条**；没开视频流时取**当前视频页的 BV 号**；都没有就隐藏互动区并提示。
- **评论按钮**：视频流开着就直接拉起评论抽屉；没开视频流则打开原视频页，同时把链接复制到剪贴板。
- 互动状态（`config.js` 的 `interact`）现在**弹窗与页面共用**，两边显示不会打架；从弹窗操作后，视频流里的右侧按钮会同步刷新。
- 新增 `test/interact.test.mjs`（11 条行为测试）：用极简 DOM 真跑 `BBDY.interact()`，覆盖点赞的**开关两个方向**、服务端 `reqUser.like` 的续接、投币 2 币与防重复、收藏用默认收藏夹并可取消、评论走抽屉、未登录拦截并唤起登录、空 BV 与未知动作不崩，以及「弹窗发的消息类型后台必须有处理」。

### 变更

- 互动请求统一由**内容脚本**执行（弹窗拿不到 csrf cookie，也拿不到评论接口需要的 aid/oid），弹窗只负责 UI；后台新增 `BB_ACTION` 转发。
- `BB_STATE` 现在额外返回 `bvid`，弹窗据此决定互动目标。

## [1.1.2] - 2026-09-23

把「找不到图标 / 没有菜单 / Service Worker 不活动」这几种情况讲清楚，并加上护栏。

### 新增

- `test/popup.test.mjs`（9 条静态检查）：manifest 合法性且无 BOM、引用文件是否齐全、`popup.js` 用到的 id 在 `popup.html` 里是否都存在、`popup.html` 控件与 `DEFAULTS` 是否对齐、`background.js` 的补注入列表与 manifest 是否完全一致、默认弹窗与快捷键是否配好、全量源码无 BOM。
- 后台在**非 B 站页面**被触发时，会在当前页飘一条提示说明去哪儿用（之前是静默失败，或者依赖未申请的 notifications 权限）。

### 说明

- **找不到扩展图标**：Chrome 默认把新装扩展收进工具栏的拼图菜单（🧩）里，需手动点钉子固定。不固定也不影响使用——顶栏「竖滑刷」才是主入口。
- **「Service Worker 不活动」**：MV3 设计如此（空闲约 30 秒停止），后台无状态，任何消息都会唤醒它，不影响功能。
- **弹窗没反应**时的排查顺序：先看弹窗底部状态栏（会显示版本与三个入口的挂载情况）→ 再在页面控制台跑 `BBDY.diag()`。

## [1.1.1] - 2026-09-23

排障体验：让「Service Worker 不活动」这类正常现象不再造成误会。

### 新增

- **页面自检**：在 B 站页面控制台执行 `BBDY.diag()`，会打印一张体检表（版本、入口位置设置、顶栏药丸是否在线、视频页入口、悬浮兜底、覆盖层状态、当前视频、播放引擎，以及顶栏/互动栏锚点是否存在）。
- **弹窗状态显示**：工具栏弹窗会主动唤醒 Service Worker 并显示真实状态，包括扩展版本与三个入口的挂载情况（顶栏入口 ✓ / 视频页入口 ✓ / 悬浮兜底）。

### 说明

- MV3 的 Service Worker 空闲约 30 秒后会被浏览器主动停止，扩展页里显示「不活动」属于**设计如此**。本扩展的后台是**无状态**的：内容脚本按声明直接注入，顶栏入口、快捷键、视频流都不依赖后台常驻；任何消息（点击弹窗、按快捷键、内容脚本通信）都会自动唤醒它。
- 因此看到「不活动」不影响功能。若确实无响应，请在 B 站页面控制台跑 `BBDY.diag()` 并把结果发出来。

## [1.1.0] - 2026-09-23

把入口从「悬浮按钮」升级成**嵌进 B 站自己的界面**。

### 新增

- **顶栏入口**：在 B 站顶栏右侧、头像左边插入一颗粉色「竖滑刷」药丸，和站内自己的入口排在同一行。所有 B 站页面都有（`extension/src/entry.js`）。
- **视频页入口**：在视频页「一键三连」那一排的末尾也加一颗，点一下直接在当前视频上开刷。
- **带上当前视频**：从视频页进入时，会把正在看的这个视频插到队列最前面先播（可在设置里关）。
- **入口位置三档**：`嵌进顶栏` / `右下角悬浮按钮` / `只要快捷键`，扩展弹窗与设置面板都能改。
- **进视频页自动开刷**：可选项，默认关闭。
- 新增 `test/entry.test.mjs`：13 条用例覆盖 BV 号解析、顶栏/互动栏的插入位置（含"必须紧挨头像左侧"）、选择器链降级、重复注入、顶栏重建后重插、以及"顶栏改版时自动退回悬浮按钮"。

### 变更

- `entryMode` 取代原 `showLauncher` 布尔值，老配置会自动迁移（`true → native`、`false → hidden`）。
- 悬浮按钮降级为**兜底**：只有在明确选择悬浮模式、或顶栏锚点找不到（B 站改版）时才出现，避免和原生入口重复。
- 入口注入用「选择器链 + MutationObserver」，顶栏/互动栏被 SPA 重建或结构改版都会自动重插，且失败时静默降级，不影响原页面。

### 修复

- 旧药丸节点在 B 站重建顶栏后会脱离文档，之前会复用这个"僵尸节点"，现在按 `isConnected` 判断并丢弃重插。

### 发布物调整

- Release 里**只放安装包**（`extension-vX.Y.Z.zip`），不再附带源码压缩包；源码始终在仓库，需要下载整份源码时用 GitHub 版本页自带的 **Source code**。
- 安装包改为「解压出来就是一个可直接加载的文件夹」（`dsb-bilibili-vertical-vX.Y.Z/manifest.json`），不用再往里点一层。
- `tools/build-release.mjs` 相应简化为只产出一个安装包。

## [1.0.0] - 2026-09-22

首个可用版本。把 bilibili.com 变成可上下滑的沉浸式竖屏视频流。

### 新增

- **竖滑内核**：三槽循环翻页（上一条 / 当前 / 下一条复用同一组 DOM），支持上下滑、鼠标滚轮、<kbd>↑</kbd><kbd>↓</kbd>、<kbd>J</kbd><kbd>K</kbd>、空格切换；翻页时当前页缩放回落、相邻页缩放进场。
- **双引擎播放**：
  - 原生 `<video>` 引擎（默认）——通过 `playurl(fnval=1)` 取带声音的整段 MP4 直链，播放/暂停/seek/倍速/静音/循环/进度条全部自控，手势因此能做得跟手；
  - 官方 iframe 外链播放器（兜底）——番剧、付费内容、接口风控、直链失效时自动切换，保证永远有画面。
- **内容来源**：首页推荐流（纯 JS 实现 WBI 签名，密钥实时取自 `nav` 接口）、热门、排行榜（分区轮换）、关注动态（需登录）。
- **队列策略**：本地去重（可设保留天数）、过滤屏蔽项、预取后续若干条的 `cid` 与播放地址、接口失败自动降级到热门。
- **交互**：单击暂停/播放（中央大图标）、双击中间点赞（飘心动画）、双击左右两侧 ±10 秒、长按弹出更多菜单、长按上拖 2x/3x 倍速、自绘进度条可点可拖 seek。
- **互动**：点赞、投币 2 枚、收藏到默认收藏夹、发表/查看评论、分享（复制链接）、关注/取关；未登录时自动唤起站内登录弹窗。
- **评论抽屉**：分页上滑加载、楼层回复、IP 属地、点赞数。
- **自绘弹幕**：`dm/list.so` XML 接口 + 轨道 CSS 动画，随播放进度推进，可开关。
- **画面填充**：竖屏裁切 / 铺满 / 完整显示三档。
- **设置面板**：内容来源、自动连播、单条循环、静音启动、弹幕、画面填充、预取条数、清晰度、本地屏蔽列表管理、账号入口。
- **扩展壳**：工具栏弹窗（开关 + 常用设置）、右下角可拖拽的悬浮入口、`Alt`+<kbd>Shift</kbd>+<kbd>B</kbd> 与 <kbd>\\</kbd> 快捷键、`Esc` 逐层退出。
- **离线演示台**（`demo/`）：零依赖本地服务，假 API + 假封面 + 现搓的可播放 MP4，不装扩展、不碰真实账号即可跑通整套交互。
- **离线自测**（`test/`）：MD5 实现与 `node:crypto` 逐字节对比、BV↔av 互转（用线上接口返回的真实 aid 校验）、卡片归一化、WBI 签名被线上推荐流接受、热门接口、playurl 清晰度。
- **图标**：`tools/gen-icons.mjs` 纯代码生成 PNG（自写 PNG 编码 + 4x 超采样抗锯齿），无外部依赖。

### 已知边界

- 外链播放器与未登录的 `playurl` 一般只给到 480P/720P；登录后清晰度更高。
- 官方外链播放器**没有公开的 JS 控制接口**，兜底模式下暂停/倍速/进度由官方控件负责。
- 受浏览器自动播放策略限制，默认静音启动，需一次点击或按 <kbd>M</kbd> 才有声音。
